// lib/theme/colors.dart
import 'package:flutter/material.dart';

/// พาเล็ตหลักของแอป
const seed = Color(0xFFED6E7F);               // ชมพูพีช (seed)
const brand = Color(0xFFEB6A79);              // ปุ่ม/จุดเน้น
const brandDark = Color(0xFFC05260);
const mint = Color(0xFF44C4A1);               // สีรอง (สำเร็จ/สถานะดี)
const cream = Color(0xFFFFF5F7);              // ฉากหลังอ่อน
const ink = Color(0xFF1E1E1E);                // ข้อความเข้ม

// ใช้ในบางคอมโพเนนต์แบบตรงๆถ้าต้องการ
const white = Colors.white;
const black = Colors.black;
